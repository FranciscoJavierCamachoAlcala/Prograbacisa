Tarea 6
1. La programacion, es el sustento de todas las personas porque, es con lo que se desarrollo nuevas forma de hacernos la vida mas comoda.

2. 1   0    0    1    1   0   1   1
 128 + 64 + 32 + 16 + 8 + 4 + 2 + 1 =155
 
 3. El numero binario es:    1      1     1      0      0       1       1          
                            1/2    3/2   7/2   14/2   28/2    57/2   115/2 
                            
El numero Hexadecimal es:  7      3
                         7/16  115/16  

5.#include <iostream>
// a) Expresión requerida para usar funciones iostream

void main(){
  float arista;
  
  cout << "Escribe la longitud de la arista del cubo: ";
  cin >> arista;
  
  float volumen;
volumen = pow(arista, 3);
  
  cout << "El volumen del cubo es " << volumen << endl;
}

6.#include <iostream>
#include <math.h>

void main(){
  float a = 5.6;
  float b = 8.4;
  float c;
  
  c = round(a); // a) 6
  c = round(b); // b) 8
  
  c = floor(b); // c) 8
  c = ceil(b); // d) 9
}

7. #include <iostream>

void main(){
  bool a = 8 <= 8;
  bool b = 4 != 5;
  bool c;
  
  c = a && a; // a) true
  c = a || a; // b) true
  
  c = a && b; // c) true
  c = b || a; // d) true
}

8.
#include <iostream>
#include <string>
using namespace std;

void main(){
  string s = "Feliz navidad";
  
  int a = // a); 0 
  int b = // b); 3 
  int c = // c); 6
  
  cout << s[a] << s[b] << s[c];
}
